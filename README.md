# FocalPoint
An LLM chat interface that uses eye-tracking to detect how you actually read — then continuously adapts response length, format, and depth to match your attention. Continual learning from implicit signals, no feedback required.
